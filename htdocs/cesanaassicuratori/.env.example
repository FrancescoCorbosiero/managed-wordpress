# ============================================
# PROJECT CONFIGURATION
# ============================================
# Nome progetto (usato per container/network/volume naming)
# Esempio: edizioni-rosi, cliente-xyz, portfolio-mario
PROJECT_NAME=frost-production
WORDPRESS_IMAGE=alpacode-wp:0.0.1

# Dominio principale (per Caddy reverse proxy)
# Esempi:
#   - example.com (produzione)
#   - staging.example.com (staging)
DOMAIN=example.com

# ============================================
# DATABASE CONFIGURATION
# ============================================
# IMPORTANTE: Usa password FORTI in produzione!
# Genera password sicure: openssl rand -base64 32

DB_NAME=wordpress_db
DB_USER=wordpress_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
DB_ROOT_PASSWORD=CHANGE_ME_STRONG_ROOT_PASSWORD_HERE

# Prefisso tabelle WordPress (default: wp_)
TABLE_PREFIX=wp_

# ============================================
# WORDPRESS CONFIGURATION
# ============================================
# Memory limits (default: 256M/512M)
WP_MEMORY_LIMIT=256M
WP_MAX_MEMORY_LIMIT=512M

# Container memory limits (default: 512M)
WP_MEMORY_LIMIT_CONTAINER=512M
WP_MEMORY_RESERVATION=256M

# Disabilita editing file da admin (security best practice)
# true = disabilita (consigliato produzione)
# false = abilita (utile development)
DISALLOW_FILE_EDIT=true

# ============================================
# MARIADB CONFIGURATION
# ============================================
# Buffer pool size (default: 128M)
# Regola in base a RAM disponibile:
#   - VPS 1GB RAM: 128M
#   - VPS 2GB RAM: 256M
#   - VPS 4GB+ RAM: 512M
MARIADB_BUFFER_POOL=256M

# Container memory limits
MARIADB_MEMORY_LIMIT=512M
MARIADB_MEMORY_RESERVATION=256M

# ============================================
# PHPMYADMIN (opzionale, solo per debug)
# ============================================
# Porta locale (accessibile solo via SSH tunnel)
PHPMYADMIN_PORT=8081

# Per avviare phpMyAdmin in produzione:
# docker compose --profile debug up -d
# Poi: ssh -L 8081:localhost:8081 user@vps.com
# Accedi: http://localhost:8081
